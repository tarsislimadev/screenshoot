name: GitHub release

on:
  schedule:
    - cron: '50 * * * *'
  push:
    branches:
      - 'main'
  workflow_dispatch:

jobs:
  release:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v3

      - name: Install dependencies 
        run: docker-compose run --user root app npm i 

      - name: Install puppeteer
        run: docker-compose run --user root app node node_modules/puppeteer/install.js

      - name: Docker build
        run: docker-compose run --user root github-release node ./app/index.js

      # - name: Permissions
      #   run: sudo chmod 1000:1000 ./src/*.png

      - name: Create release
        run: gh release create v$( date +%Y-%m-%d-%H-%M ) ./src/*.png
        env:
            GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

